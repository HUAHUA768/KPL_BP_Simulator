# KPL BP 模拟器 — 实现思路

## 一、项目定位

> **目标导向**：让面试官看到你懂后端全链路，但项目本身不能太大导致写不完。

- ✅ 有 HTTP RESTful API + WebSocket 实时推送
- ✅ 有 MySQL 数据持久化 + Redis 缓存
- ✅ 有 Docker 一键部署
- ❌ 不做微服务、消息队列等过度设计

---

## 二、技术选型

| 层级 | 技术 | 原因 |
|------|------|------|
| 前端 | TypeScript + React (Vite) | 校招标配，上手快 |
| 后端 | **Go** (Gin) | 轻量、并发好、简历加分 |
| 数据库 | **MySQL** | 校招面试必问 |
| 缓存 | **Redis** | 存房间状态、session，体现懂缓存 |
| 通信 | RESTful + WebSocket | BP 实时对战需要 WS 推送 |
| 部署 | **Docker** + docker-compose | 本地一键启动，简历写"熟练容器化" |

### 为什么这么选？
- Go + MySQL + Redis 是国内中小厂后端校招最常见的技术组合
- 单机就能跑，`docker-compose up` 全部起来，不需要云服务器
- 项目体感真实：有 HTTP 接口、有 WebSocket 推送、有数据库读写、有缓存

### 不推荐的
- ~~Java Spring Boot~~：太重，学习曲线陡，时间不够
- ~~微服务/消息队列~~：单机项目硬上这些反而露怯
- ~~k8s~~：没必要，面试官知道你没真用过

### 一句话总结

> **前端 React + 后端 Go + MySQL + Redis + Docker，全部 docker-compose 本地跑通。**
> 这就是一份合格的校招后端项目简历。

---

## 三、开发步骤（按优先级排序）

### 阶段一：数据库设计（第 1 天）

1. **画 ER 图**：明确实体关系
   - `hero`（英雄表）— 英雄基础信息
   - `team`（战队表）— 参赛队伍
   - `match_record`（对抗记录表）— 一场 BO/X 的整体对局
   - `game`（单局表）— 每一局独立的比赛
   - `ban_pick_action`（BP 操作表）— 每一步 ban/pick 记录

2. **写建表 SQL**：确定字段类型、索引、主外键约束
   - 参考文件：`backend/migrations/001_create_tables.sql`

3. **编写种子数据脚本**：初始化英雄池
   - 参考文件：`backend/migrations/002_seed_data.sql`
   - 覆盖对抗路、中路、发育路、打野、游走五大分路

### 阶段二：后端核心逻辑（第 2~3 天）⭐ 最重要

#### 2.1 BP 状态机（灵魂模块）

> **决策：枚举 + switch/select 分支驱动**

| 方案 | 适用场景 | 本项目选择 |
|------|---------|-----------|
| 枚举 + switch | 步骤固定、数量有限、需要 exhaustive check | ✅ **采用** |
| 函数指针表 / 状态表 | 流程可变、多赛制切换、需要高度扩展 | ❌ 暂不采用 |

**为什么选枚举 + switch？**
- KPL BP 规则是**固定的 20 步**，不会频繁变化
- Go 的 `switch` 有 **exhaustive check**（漏 case 编译报错），天然适合做状态机
- 代码直观，调试时日志清晰，面试时也容易解释
- 如果未来要加新赛制，可以把 switch 抽成独立的 `getTurnInfo()` 数据映射函数，核心逻辑不变

**实际设计模式 — 混合方案：**

```
┌─────────────────────────────────────────────┐
│  Engine.ExecuteAction(action, heroID)       │ ← handler 层调用
│    ├─ 校验 actionType 是否匹配当前 turn      │
│    ├─ 委托 validator.Validate()             │ ← 接口隔离
│    └─ switch(actionType):                   │
│         Ban → handleBan(side, heroID)       │ ← 独立函数
│         Pick → handlePick(side, heroID)     │ ← 独立函数
│    └─ Advance() → CurrentTurn++             │ ← 纯数据推进
└─────────────────────────────────────────────┘

getTurnInfo(turn) → TurnInfo{Round, Side, Action, Index}
// 纯数据映射函数，不含业务逻辑，极易测试
```

| 步骤 | 轮次   | 阶段 | 行动方 | 操作内容       |
|------|--------|------|--------|----------------|
| 1    | 第一轮 | Ban  | 蓝方   | 禁用 1 个英雄  |
| 2    | 第一轮 | Ban  | 红方   | 禁用 1 个英雄  |
| 3    | 第一轮 | Ban  | 蓝方   | 禁用 1 个英雄  |
| 4    | 第一轮 | Ban  | 红方   | 禁用 1 个英雄  |
| 5    | 第一轮 | Pick | 蓝方   | 选择 1 个英雄  |
| 6    | 第一轮 | Pick | 红方   | 选择 1 个英雄  |
| 7    | 第一轮 | Pick | 红方   | 选择 1 个英雄  |
| 8    | 第一轮 | Pick | 蓝方   | 选择 1 个英雄  |
| 9    | 第一轮 | Pick | 蓝方   | 选择 1 个英雄  |
| 10   | 第一轮 | Pick | 红方   | 选择 1 个英雄  |
| 11   | 第二轮 | Ban  | 红方   | 禁用 1 个英雄  |
| 12   | 第二轮 | Ban  | 蓝方   | 禁用 1 个英雄  |
| 13   | 第二轮 | Ban  | 红方   | 禁用 1 个英雄  |
| 14   | 第二轮 | Ban  | 蓝方   | 禁用 1 个英雄  |
| 15   | 第二轮 | Ban  | 红方   | 禁用 1 个英雄  |
| 16   | 第二轮 | Ban  | 蓝方   | 禁用 1 个英雄  |
| 17   | 第二轮 | Pick | 红方   | 选择 1 个英雄  |
| 18   | 第二轮 | Pick | 蓝方   | 选择 1 个英雄  |
| 19   | 第二轮 | Pick | 蓝方   | 选择 1 个英雄  |
| 20   | 第二轮 | Pick | 红方   | 选择 1 个英雄  |

**关键设计要点：**
1. `const iota` 定义 20 个 BPTurn（每步唯一 ID）
2. `switch` 驱动状态流转 + exhaustive check
3. ban/pick 执行逻辑抽成独立 `handleBan/handlePick` 函数
4. 校验逻辑通过 `Validator` 接口解耦，方便 mock 测试
5. `getTurnInfo()` 纯数据函数，turn→元信息的一一映射

参考文件：`backend/internal/service/bp_engine.go`、`bp_engine_test.go`

- 写单元测试验证状态转换是否正确（`bp_engine_test.go`）

#### 2.2 RESTful API

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/heroes` | GET | 获取英雄列表（可过滤已 ban/pick） |
| `/api/rooms` | POST | 创建 BP 房间 |
| `/api/rooms/:id/join` | POST | 加入房间 |
| `/api/rooms/:id/ban` | POST | 执行 ban 操作 |
| `/api/rooms/:id/pick` | POST | 执行 pick 操作 |
| `/api/rooms/:id/status` | GET | 获取当前 BP 状态 |
| `/api/games` | GET | 查询历史对局 |

#### 2.3 分层架构

```
backend/internal/
├── handler/       # HTTP/WebSocket 处理器（接收请求、返回响应）
├── service/       # 业务逻辑层（BP 状态机为核心）
├── repository/    # 数据访问层（MySQL CRUD）
├── model/         # 数据模型（ORM struct）
├── middleware/    # 中间件（CORS、日志）
└── websocket/     # WebSocket 管理（Hub + Client）
```

**开发顺序**：service → repository → handler → main.go 整合

### 阶段三：WebSocket 实时推送（第 4 天）

- 替换 HTTP 轮询，建立 WS 长连接
- 前后端通过 WS 推送 BP 操作结果
- Hub 管理房间连接，Client 管理单个连接
- 参考文件：`backend/internal/websocket/hub.go`, `client.go`

### 阶段四：前端基础界面（第 5~6 天）

| 页面 | 功能 |
|------|------|
| HomePage | 创建/加入房间 |
| BPRoom | BP 主界面（英雄展示面板、操作区、状态显示） |
| HistoryPage | 历史对局查看 |

- 英雄展示面板、BP 操作区、状态显示
- 调后端 API 拿到数据渲染
- **先做功能跑通，不做动画和炫酷效果**

### 阶段五：加 Redis（第 7 天）

- 存房间临时状态（加速读取，减轻 MySQL 压力）
- 存用户 session
- 体现你对缓存的意识

### 阶段六：Docker 部署（第 8 天）

- 写 `Dockerfile.backend` + `Dockerfile.frontend`
- 写 `docker-compose.yml` 一键启动全部服务
- 作为项目收尾和简历包装

---

## 四、BP 规则详解

### 标准 BP 流程（两轮，共20步）

```
┌─────────────────────────────────────────────────┐
│          第一循环（Blue先Ban），共10步            │
├─────────────────────────────────────────────────┤
│  Ban: 🔵蓝 → 🔴红 → 🔵蓝 → 🔴红                  │
│  Pick: 🔵蓝 → 🔴红 → 🔴红 → 🔵蓝 → 🔵蓝 → 🔴红    │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│          第二循环（Red先Ban），共10步             │
├─────────────────────────────────────────────────┤
│  Ban: 🔴红 → 🔵蓝 → 🔴红 → 🔵蓝 → 🔴红 → 🔵蓝     │
│  Pick: 🔴红 → 🔵蓝 → 🔵蓝 → 🔴红                  │
└─────────────────────────────────────────────────┘
```

### 详细步骤序列（共 20 步）

| 步序 | 阶段 | 操作方 | 动作 | 说明 |
|------|------|--------|------|------|
| 1 | 第一轮-Ban | 🔵 蓝队 | Ban | 蓝ban |
| 2 | 第一轮-Ban | 🔴 红队 | Ban | 红ban |
| 3 | 第一轮-Ban | 🔵 蓝队 | Ban | 蓝ban |
| 4 | 第一轮-Ban | 🔴 红队 | Ban | 红ban |
| 5 | 第一轮-Pick | 🔵 蓝队 | Pick | 蓝pick |
| 6 | 第一轮-Pick | 🔴 红队 | Pick | 红pick |
| 7 | 第一轮-Pick | 🔴 红队 | Pick | 红pick |
| 8 | 第一轮-Pick | 🔵 蓝队 | Pick | 蓝pick |
| 9 | 第一轮-Pick | 🔵 蓝队 | Pick | 蓝pick |
| 10 | 第一轮-Pick | 🔴 红队 | Pick | 红pick |
| 11 | 第二轮-Ban | 🔴 红队 | Ban | 红ban |
| 12 | 第二轮-Ban | 🔵 蓝队 | Ban | 蓝ban |
| 13 | 第二轮-Ban | 🔴 红队 | Ban | 红ban |
| 14 | 第二轮-Ban | 🔵 蓝队 | Ban | 蓝ban |
| 15 | 第二轮-Ban | 🔴 红队 | Ban | 红ban |
| 16 | 第二轮-Ban | 🔵 蓝队 | Ban | 蓝ban |
| 17 | 第二轮-Pick | 🔴 红队 | Pick | 红pick |
| 18 | 第二轮-Pick | 🔵 蓝队 | Pick | 蓝pick |
| 19 | 第二轮-Pick | 🔵 蓝队 | Pick | 蓝pick |
| 20 | 第二轮-Pick | 🔴 红队 | Pick | 红pick |

### 各队最终阵容统计

| 队伍 | Ban 数量 | Pick 数量 |
|------|---------|----------|
| 🔵 蓝队 | 6 (ban×6) | 5 (pick×5) |
| 🔴 红队 | 5 (ban×5) | 5 (pick×5) |

> **注意**：状态机按步序驱动，每步对应一个固定的操作方和动作类型。
> 代码中用 `const iota` 定义了 20 个 BPTurn，`getTurnInfo()` 纯数据映射函数返回每一步的 Round/Side/Action/Index。

---

### 英雄可用性规则（不可 ban / pick 的原因区分）

#### 核心设计：两队共享一个 HeroPool

**两队不需要分开的 HeroPool**，共享一个池才是正确的建模方式：

- 对方 pick 了 X → X 从**共享池**移除 → 本方天然无法再 ban/pick X（"对面已使用"由共享池机制保证）
- 本方 ban 了 X → X 从共享池移除 → 对方无法 pick X
- 任何被 ban 或被 pick 的英雄，双方都不能再用

#### 英雄的 7 种状态（`HeroStatus` 枚举）

| 状态 | 含义 | 可被 ban | 可被 pick |
|------|------|---------|----------|
| `HeroAvailable` | 可用 | ✅ | ✅ |
| `HeroTournamentBanned` | 赛事禁用（如雅典娜） | ❌ | ❌ |
| `HeroBannedByBlue` | 已被蓝方禁用 | ❌ | ❌ |
| `HeroBannedByRed` | 已被红方禁用 | ❌ | ❌ |
| `HeroPickedByBlue` | 已被蓝方选择 | ❌ | ❌ |
| `HeroPickedByRed` | 已被红方选择 | ❌ | ❌ |
| `HeroUnknown` | 英雄ID不存在 | ❌ | ❌ |

**判定顺序**（`heroStatus()` 方法）：
```
赛事禁用 → 已被禁用 → 已被选择 → 是否在池 → 未知
```

#### 错误信息设计（后端）

`handleBan` / `handlePick` 不再返回笼统的"不在可用池"，而是区分具体原因：

| 状态 | 错误信息 |
|------|---------|
| 赛事禁用 | `英雄X为赛事禁用英雄，不可ban/pick` |
| 已被本方/对方禁用 | `英雄X已被禁用，不可ban/pick` |
| 已被对方选择 | `英雄X已被蓝方/红方选择，不可ban/pick` |
| 不存在 | `英雄X不存在，不可ban/pick` |

#### 前后端配合（前端渲染逻辑）

| 场景 | 前端表现 |
|------|---------|
| 情况① 赛事禁用（雅典娜） | `GET /api/heroes` 直接过滤掉，**BP 界面完全不显示该英雄** |
| 情况② 对方已使用 | 后端推送中携带已选英雄列表，前端将该英雄渲染为**灰色、不可点击** |
| 已被双方禁用 | 显示在禁用区，灰色不可点击 |

---

## 五、关键技术决策

### Redis 使用场景
- **房间状态缓存**：高频读取的房间 BP 状态走 Redis，降低 MySQL 压力
- **Session 存储**：用户登录态、房间加入凭证
- **最终一致性**：每次操作写入 MySQL 后同步更新 Redis

### Docker 编排
```yaml
# docker-compose.yml 服务清单
services:
  mysql:        # 数据库
  redis:        # 缓存
  backend:      # Go 后端（依赖 mysql + redis）
  frontend:     # React 前端（Nginx 托管）
```

### 开发节奏建议

| 阶段 | 天数 | 产出 |
|------|------|------|
| 数据库 + 种子数据 | 1 天 | 建表 SQL + 英雄初始数据 |
| 后端核心（状态机+API） | 2~3 天 | RESTful API 可调用 |
| WebSocket 推送 | 1 天 | 实时对战能力 |
| 前端页面 | 2 天 | 基本可用界面 |
| Redis + Docker | 2 天 | 完整部署方案 |

> **核心原则**：先完成后端核心（BP 状态机），再补外围。
> Redis 和 Docker 是优化阶段加，不是开发阶段就折腾。
